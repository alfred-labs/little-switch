# Monotonically increasing across releases: Sparkle compares CFBundleVersion
# ("sparkle:version") across every release, so BUILD_NUMBER must never decrease
# between shipped versions, including when MARKETING_VERSION changes. The
# app's LSBuildTag still carries `git describe` for diagnostics.
MARKETING_VERSION=0.4.8
BUILD_NUMBER=20
